1. Updated OpenAI integration to use the Responses API (`openai_inference/responses.py`).
2. Updated app inference wiring and token parameter naming (`max_output_tokens`).
3. Updated default model recommendation to `gpt-5.2`.

